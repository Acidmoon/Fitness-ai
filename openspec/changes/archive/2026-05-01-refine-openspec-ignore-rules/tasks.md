## 1. Ignore Rule Update

- [x] 1.1 Narrow `.gitignore` rules so active OpenSpec working changes remain ignored while archive content is allowed

## 2. Verification

- [x] 2.1 Validate that an active change path is ignored
- [x] 2.2 Validate that an archived change path is not ignored and can be staged normally
- [x] 2.3 Validate the OpenSpec change before archiving
